{
  "name": "batch-uglify-js",
  "version": "0.0.0",
  "description": "",
  "bin": {
    "batchuglifyjs": "bin/batchuglifyjs"
  },
  "scripts": {
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "postinstall": "npm run build"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "uglify-js": "^3.6.0",
    "typescript": "^3.5.1",
    "@types/node": "^12.0.5",
    "@types/uglify-js": "^3.0.4"
  },
  "devDependencies": {
    "nodemon": "^1.19.1",
    "ts-node": "^8.2.0"
  }
}
